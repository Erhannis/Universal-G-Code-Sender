Is "probe" (G38.2) really in absolute coords????  the code says "distance"
Check //UNTESTED periodically
Move "offset" values into settings
Reverse probe for compensate backlash?

visualization
  outside corner with negative values gives weird render
  update corners with push?
  measure angle
  (align with angle?)
  center
    inside, outside
  pitch
  trace running machine
  show sent gcode
  fix the rotation

Probe intermittently goes in the wrong direction.  Seems consistent in a given program run?

features
  *measure angle
  align with angle???
    (probably extensive changes, since grbl doesn't support rotation)
    (maybe I could restrict the scope)
    how does the autolevel do it?
  center
    of (circle, line)
    x (inside, outside)
    Permit manual jogging between sides?
  pitch
  more manual measurements - store as variables etc.
    extend rotation to rest of pgm?  o_o
    support 3dof rotation?
  shape scanning??

external features?
  bore
    (shell, solid)
    x
    (cylinder, square, cube?)
    in multiple layers?  down, then out, down, then out?
    progressive sizes, horizontal
  done:
    cylinder x shell
    cube x solid

probe
  x, y, z
  arbitrary dir

lathe
  probe find center against flat surface jig thing?
  *round facing
  *flat facing
  *taper
    hmm - permit define offset to surface or something?
    permit max Z
  FIX: make taper go point first, don't drag shallow angle
  chamfer
    a form of taper?
  round corner
  generally retract away from all faces
  notch
    trapezoidal
  

goto x zero, y zero, z zero

standardize directions (annotate labels)
